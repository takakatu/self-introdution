.card
  .card-top
    = markdown(@post.title)
    = @post.created_at.strftime("%Y年%m月%d日")
  .card-body
    .card-img
      = image_tag @post.title_image_url
    .card-text
      = markdown(@post.content)